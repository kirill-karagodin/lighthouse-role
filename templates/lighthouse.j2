server {
    listen       80;
    server_name  localhost;

    access_log {{ lighthouse_log_dir }}/{{ lighthouse_log_name }}_access.log;
    error_log {{ lighthouse_log_dir }}/{{ lighthouse_log_name }}_error.log;

    location / {
        root   {{ lighthouse_dir }};
        index   index.html;
    }
}
